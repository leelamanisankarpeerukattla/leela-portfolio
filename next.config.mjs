/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true
  }
};

// For GitHub Pages or a pure static export, add the following options before building:
// output: 'export',
// trailingSlash: true,

export default nextConfig;
